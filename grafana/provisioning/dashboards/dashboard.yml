apiVersion: 1

providers:
  - name: "Kafka RPS"
    orgId: 1
    folder: "Kafka"
    type: file
    disableDeletion: false
    editable: true

    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false